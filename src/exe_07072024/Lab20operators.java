package exe_07072024;

public class Lab20operators {
    public static void main(String[] args) {
        int age = 65;
        int lambo = -1;
        //Datatype VariableName(identifier)=Variable value(Literals)
        // we can have three operators first one is unary it means 1 operand and 1 operations.
        //lambo operand -unary value and = would be operators same for int age=65 (plus would be unary value)

    //Binary operator
    int a = 25;
    int b = 25;
    int sum = a + b;
    // a and b is the operand and + is the operator note: sum is also operand but main function is a+b

    //third one is turnary operator so java says if you want to perform with three operator
    // you can use it like a,b,c so abc are operand
}
}
